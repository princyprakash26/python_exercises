# area and volume:

import math

radius=float(input('enter the value of radius:'))
area=math.pi*radius*radius
volume=4/3*math.pi*(radius**3)

print(f'Area of circle {area} and volume of the sphere {volume}')